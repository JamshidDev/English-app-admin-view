server {
    listen 80;
    server_name admin.jamacoder.uz;

    root /var/www/English-app-admin-view;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
